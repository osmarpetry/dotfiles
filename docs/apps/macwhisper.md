# MacWhisper

Config export/import mechanism unverified — needs manual investigation (check `~/Library/Preferences` and `~/Library/Application Support` for a MacWhisper plist/config the way `clop/restore_clop_prefs.py` does for Clop). Nothing invented here; install fresh via the `macwhisper` cask and reconfigure by hand until this is investigated.
