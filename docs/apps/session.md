# Session

`/Applications/Session.app` — encrypted messenger (getsession.org). Cask:
`session`. No transferable config: your account is a generated Session ID,
tied to a recovery phrase you hold yourself, not to any file on disk this
repo could back up. Reinstall and restore via your recovery phrase by hand.
